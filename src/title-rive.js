/* Rive title integration was removed in favor of a code-based SVG morph
   (see src/letters.js + src/title-glyphs.js). This file is intentionally
   left as a no-op so nothing imports the Rive runtime. */
export {};
